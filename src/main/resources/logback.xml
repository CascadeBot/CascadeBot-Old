<configuration>

    <variable scope="context" name="logPattern" value="[%d{yyyy.MM.dd HH:mm}] [%t] [ %-5level] %msg%n"/>

    <appender name="LOG" class="ch.qos.logback.core.FileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <file>latest.log</file>
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>
    <appender name="DEBUG-LOG" class="ch.qos.logback.core.FileAppender">
        <file>debug-latest.log</file>
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>[%d{yyyy.MM.dd HH:mm}] [%t] [ %highlight(%-5level)] %msg%n</pattern>
        </encoder>%highlight(%-5level)
    </appender>
    <root level="DEBUG">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="LOG"/>
        <appender-ref ref="DEBUG-LOG" />
    </root>
</configuration>