language: java

dist: trusty
sudo: true

# Get latest install-jdk.sh script
before_install:
  - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh

install: sudo sh install-jdk.sh -F 10 -C

script:
  - mvn test -B

cache:
  directories:
  - $HOME/.m2
